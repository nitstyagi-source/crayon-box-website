"use client";

import React from "react";
import { AlgorithmicTimetableDesk } from "@/components/innovations/AlgorithmicTimetableDesk";

export default function TimetableGeneratorPage() {
  return (
    <div className="p-6 sm:p-8 max-w-7xl mx-auto">
      <AlgorithmicTimetableDesk />
    </div>
  );
}
